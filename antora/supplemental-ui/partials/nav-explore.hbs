{{!--
  The explore element allows switching between components and branches or tagged versions.
  Because we publish just the latest main branch for each component it's not very useful.
  Let's take it out. Default version is here:
  https://gitlab.com/antora/antora-ui-default/-/blob/master/src/partials/nav-explore.hbs
--}}
